# Google Gemini API Key (required)
# Get yours at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Firebase — Option A: Use environment variables (recommended, no JSON file needed)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Firebase — Option B: service account JSON file path (if you prefer not to use env vars)
# FIREBASE_SERVICE_ACCOUNT_PATH=your-firebase-adminsdk.json

# Server port (default: 3000)
PORT=3000
